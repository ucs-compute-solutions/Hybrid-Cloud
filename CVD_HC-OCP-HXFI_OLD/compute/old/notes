    # # Another option
    # - name: Register dummy host with variable
    #   add_host:
    #     name: "DUMMY_HOST"
    #     intersight_api_info: *intersight_api_login_info
    # - debug:
    #     msg: "API Info is: {{ intersight_api_login_info }}"


    
      #when: ansible_facts['account_ownership_state'] != 'Claimed'
      #when: hostvars['DUMMY_HOST']['account_ownership_state'] != 'Claimed'

      # - name: Show an attribute value - account state
#   debug:
#     msg: "CLAIM_TARGET_DUMMY_HOST: {{ hostvars['DUMMY_HOST']['account_ownership_state'] }}"

# - name: Show an attribute value - intersight api info
#   debug:
#     msg: "CLAIM_TARGET_DUMMY_HOST: {{ hostvars.DUMMY_HOST.intersight_api_info }}"



    # Storage Network under a policy = cluster name with incudes org, descr, storage vlan name & vlan ID
   
    # HyperFlex Cluster Profile - Screen 4 - Nodes Configuration
    # Cluster Management IP
    # Mac Prefix Address
    # Replication Factor


  #   # HyperFlex Cluster Profile - Screen 2 - Nodes Assignment
  #   - name: "Prompt to assign"
  #     pause:
  #       prompt: "Proceed with physical node assignment? (yes/no)"
  #       echo: yes
  #     register: assign_response
  #     run_once: true
  #     tags: ['prompt_assign']

  #   # Assign servers to cluster profile and set deployment action
  #   - import_role:
  #       name: policies/hyperflex_policies/node_profiles
  #     tags: ['assign']
  #     when: assign_response.user_input|bool

 

  
    # - name: Print all available Ansible facts
    #   ansible.builtin.debug:
    #     var: ansible_facts

Tags:

  tags: 
    - organization
    - never

  tags: [never, debug]



  #   # Next Screen --> Summary -> Validate/Validate & Deploy
  #   - debug:
  #       msg: "All policies and the HyperFlex cluster profile have been created."


  # # Deploy
  #   - name: "Prompt to deploy"
  #     pause:
  #       prompt: "Proceed with cluster deployment? (yes/no)"
  #       echo: yes
  #     register: deploy_response
  #     run_once: true
  #     tags: ['prompt_deploy']

  #   # Set cluster profile deployment action
  #   - import_role:
  #       name: policies/hyperflex_policies/deploy
  #     tags: ['deploy']
  #     when: deploy_response.user_input|bool

  #   - debug:
  #       msg: "HyperFlex cluster creation is complete."